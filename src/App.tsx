import { FC } from 'react'

const App: FC = () => {
  return (
    <div className="mx-auto flex h-screen max-w-prose items-center justify-center text-center text-2xl">
      React + TypeScript + TailwindCSS starting template
    </div>
  )
}

export default App
